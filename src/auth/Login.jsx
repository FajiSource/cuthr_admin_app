import AuthForm from "../components/form/AuthForm";
import Logo from "../components/ui/Logo";

export default function Login() {

    return (
        <section className="text auth-section">
            <AuthForm />
        </section>
    );
}